GEMINI_API_KEY=your_gemini_api_key_here

# Comma-separated list of extra allowed origins for CSRF check.
# Required when accessing via a reverse proxy (e.g. exe.dev).
# Example: ALLOWED_ORIGINS=https://myvm.exe.xyz:5173
ALLOWED_ORIGINS=
